const state = {
  sideBarStatus: false,
};

const mutations = {
  setSideBar(state, status) {
    state.sideBarStatus = status;
  },
};

const actions = {};

export default {
  state,
  mutations,
  actions,
};
